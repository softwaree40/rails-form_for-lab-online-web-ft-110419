  
<%= link_to "See All Classes", school_classes_path %>
<br>
<%= link_to "Add a Student", new_student_path %>

<h1>Students</h1>
<ul>
  <% @students.each do |student| %>
    <li>
      <%= link_to student.to_s, student_path(student) %>
    </li>
  <% end %>
</ul>